// Merged into SignupPatient.tsx — this file is kept for reference only.
import { Navigate } from "react-router-dom";
const VoiceSignup = () => <Navigate to="/signup/patient" replace />;
export default VoiceSignup;
