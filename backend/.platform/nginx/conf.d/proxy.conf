# Elastic Beanstalk nginx override
# Raise the upload limit so large prescription images are not rejected by nginx
# before the request reaches Node.js.
# CloudFront itself has a hard 20 MB ceiling, so 50m is plenty of headroom.
client_max_body_size 50m;
